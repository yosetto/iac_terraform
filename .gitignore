# Local .terraform directories
.terraform/

# Terraform state files
*.tfstate
*.tfstate.*

# Crash logs
crash.log
crash.*.log

# Variable override files (sensitive)
*.tfvars
*.tfvars.json

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Ignore lock files if you don’t want to pin provider versions
# (optional, but usually keep terraform.lock.hcl in git!)
# terraform.lock.hcl
